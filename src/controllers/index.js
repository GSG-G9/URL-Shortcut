const {
  getUserHandler, addUserUrlHandler, logInUser,
} = require('./user');
const { urlShortHandler } = require('./url');

module.exports = {
  getUserHandler,
  addUserUrlHandler,
  logInUser,
  urlShortHandler,
};
